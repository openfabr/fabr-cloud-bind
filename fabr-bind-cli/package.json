{
  "name": "fabr-admin-cli",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "bin": {
    "fabr": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && npm run copy-lib-src && npm run copy-templates",
    "copy-lib-src": "rm -rf dist/libs-src && mkdir dist/libs-src && cp -Rf src/libs dist/libs-src/typescript",
    "copy-templates": "rm -rf dist/client-templates && mkdir dist/client-templates && cp -Rf client-templates dist",
    "start": "node dist/index.js",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Janaka Abeywardhana",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.11",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^10.0.3",
    "commander": "^10.0.0",
    "handlebars": "^4.7.7"
  }
}
